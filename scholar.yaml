enabled: true
order:
  by: folder
  dir: asc
style: metal
toolbar:
  breadcrumbs: true
  search: true
  navigation: true
cache:
  metadata: "persist"
  content: "persist"
routes:
  data: "/data"
  search: "/search"
schema:
  default: CreativeWork
  types:
    - name: index
      schema: CollectionPage
      children: true
    - name: article
      schema: Article
    - name: blog
      schema: Blog
      children:
        - name: post
          schema: BlogPosting
    - name: book
      schema: Book
      children:
        - name: chapter
          schema: Chapter
    - name: sequence
      schema: Drawing
flexsearch:
  enabled: true
  index:
    profile: speed
    encode: icase
    tokenize: strict
    cache: true
    async: true
  full:
    profile: balance
    encode: advanced
    tokenize: full
    cache: true
    async: true
