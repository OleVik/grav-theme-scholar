{% extends page.topParent.template == 'docs' ? 'docs.html.twig' : 'default.html.twig' %}

{% block article %}
{# {{ page.content }} #}
{{ dump(page.parent.title) }}
{{ page.parent.title }}
{% endblock %}